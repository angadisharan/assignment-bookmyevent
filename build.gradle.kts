// Root build file for the multi-project build. Subprojects define their own plugins and dependencies.
allprojects {
    repositories {
        mavenCentral()
    }
}

subprojects {
    group = "com.bookmyevent"
    version = "0.0.1-SNAPSHOT"
}

// Root build file for the multi-project build. Subprojects define their own plugins and dependencies.
allprojects {
    repositories {
        mavenCentral()
    }
}

subprojects {
    group = "com.bookmyevent"
    version = "0.0.1-SNAPSHOT"
}
